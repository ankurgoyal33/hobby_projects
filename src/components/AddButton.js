import React from 'react';

export default class AddButton extends React.Component {

    render(){
        return (
            <div>
                <p>Add New Project by clicking: <button>Add</button> </p>
            </div>
        );
    }
}
